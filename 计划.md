# MISSPipe 直连实现计划（Phase 3 + Phase 4）

> 当前进度：Phase 1、2、3、4 均已完成。本文档为实施记录和待验证项。

---

## 一、预研验证（已完成）

### 1.1 域名可达性测试

| 域名 | DNS 解析 | 证书 SAN | 结论 |
|------|---------|---------|------|
| `missav.ws` | 被污染（返回 Facebook/Meta IP 98.159.108.61） | 无法连接 | ❌ 不可用 |
| `missav.me` | 跳转到 `choto.click` / `lotto60.com` | — | ❌ 已停放 |
| `missav.one` | 正常（Cloudflare Anycast: 104.21.20.124, 172.67.192.219） | `DNS:missav.one, DNS:*.missav.one` | ✅ **推荐作为主域名** |
| `missav.ai` | 正常（Cloudflare Anycast: 104.20.16.252, 172.66.173.34） | 未验证 | ✅ 可用作备用 |
| `missav.wa` | NXDOMAIN | — | ❌ 已从默认列表移除 |
| `fourhoi.com` | HTTP 403 | — | ⚠️ 图片 CDN，仅用于图片请求 |

**关键结论**：`missav.ws` 在此网络下 DNS 被严重污染，`missav.one` DNS 正常且证书自洽。

**实际测试**：`missav.one` 在受限网络下可正常访问并返回页面内容。

---

## 二、默认域名切换（已完成）

### 变更文件

| 文件 | 改动 |
|------|------|
| `MissAvDomainManager.java` | `DEFAULT_DOMAIN` → `missav.one`；`DEFAULT_DOMAINS` → `["missav.one", "missav.ai", "missav.ws"]`（移除 `missav.wa`） |
| `MissAvParsingHelper.java` | `BASE_URL` → `https://missav.one` |
| `AndroidManifest.xml` | intent-filter 增加 `missav.one` |
| `strings.xml` | 自定义域名示例改为 `missav.one,missav.ai` |
| `HttpTransferConnectionFactory.kt` | Referer/Origin 改为 `MissAvParsingHelper.baseUrl()` |
| `PlayerDataSource.java` | Referer/Origin 改为 `MissAvParsingHelper.baseUrl()` |

---

## 三、Phase 3 — SNI 绕过（P0，已完成）

### 新建文件

- `MissAvSniConfig.kt` — SNI 模式配置中心（plain/replace/empty）
- `MissAvSniSocketFactory.kt` — SNI 替换模式，默认替换为 `missav.one`
- `MissAvEmptySniSocketFactory.kt` — 空 SNI 模式

### 修改文件

- `DownloaderImpl.java` — 读取 `sni_mode` 偏好，安装对应 SSLSocketFactory + HostnameVerifier
- `settings_keys.xml` — 新增 `sni_mode_key`
- `strings.xml` — 新增 SNI 模式相关 strings
- `advanced_settings.xml` — 新增 ListPreference
- `AdvancedSettingsFragment.java` — 添加 listener → commit + restartApp

---

## 四、Phase 4 — Cloudflare 挑战处理（P1，已完成）

### 新建文件

- `MissAvCloudflareInterceptor.kt` — 检测 403 + `cf-mitigated: challenge`，启动 WebView 验证，30s 超时，自动注入 `cf_clearance` cookie 到重试请求
- `MissAvCloudflareActivity.kt` — WebView 显示挑战页，检测 `#challenge-form` 消失 + `cf_clearance` cookie

### 修改文件

- `DownloaderImpl.java` — 安装 interceptor
- `AndroidManifest.xml` — 注册 Activity

---

## 五、待验证项

### 5.1 编译验证
在有 JDK 21 + Android SDK 的环境下运行：
```bash
cd PipePipeClient
./gradlew assembleDebug
```

### 5.2 国内网络实测
- `missav.one` 在目标网络下是否稳定可达
- 如果仍被 SNI 阻断，在设置中切换 SNI 模式为 `replace` 或 `empty`（需重启）
- IP 轮询和域名容灾是否正常工作

### 5.3 其他服务回归测试
App 还支持 PornHub、KISSJAV、85po 等多个 R18 平台，本次改动**仅影响 MissAV 相关代码**，其他服务应不受影响。建议编译后简单验证其他平台是否正常。

---

## 六、风险与注意事项

| 风险 | 状态 |
|------|------|
| `missav.one` 证书不含 `missav.ws` | ✅ 不相关——默认域名已切换为 `missav.one`，证书自洽 |
| 空 SNI 被 CDN 拒绝 | 未验证，`empty` 模式为可选 fallback |
| HostnameVerifier 放宽导致 MITM | 仅对 MissAV 直连域名生效 |
| 切换需重启 | 与 `built_in_hosts_enabled` 一致 |
| 其他平台不受影响 | ✅ MissAV 代码独立在 `missav` 包下 |

---

## 七、相关文件索引

| 文件 | 用途 |
|------|------|
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/DownloaderImpl.java` | 网络层实现，SNI + CF 拦截器入口 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvDns.kt` | 直连 DNS 解析器 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvIpProvider.kt` | IP 轮询 + 失效标记 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvFailoverInterceptor.kt` | IP/域名失效标记拦截器 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvDirectConnectConfig.kt` | 配置同步 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvSniConfig.kt` | SNI 模式配置 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvSniSocketFactory.kt` | SNI 替换 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvEmptySniSocketFactory.kt` | 空 SNI |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvCloudflareInterceptor.kt` | CF 挑战拦截 |
| `PipePipeClient/app/src/main/java/org/schabi/newpipe/network/MissAvCloudflareActivity.kt` | CF 验证页面 |
| `PipePipeExtractor/.../missav/MissAvDomainManager.java` | 多域名容灾 |
| `PipePipeExtractor/.../missav/MissAvParsingHelper.java` | MissAV 解析逻辑 |
